import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Opcional: Habilitar cabeçalhos personalizados para Service Worker se necessário
};

export default nextConfig;
